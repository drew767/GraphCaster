{
  "name": "graphcaster-embed",
  "private": true,
  "version": "0.1.0",
  "description": "GraphCaster embeddable chat widget — vanilla DOM, no framework",
  "type": "module",
  "scripts": {
    "build": "vite build --config vite.embed.config.ts",
    "test": "vitest run --config vitest.embed.config.ts"
  },
  "engines": {
    "node": ">=20.19"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "jsdom": "^24.0.0",
    "typescript": "~5.6.3",
    "vite": "^6.0.6",
    "vitest": "^3.0.0"
  }
}
