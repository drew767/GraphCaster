# graph-caster (Python)

Интерпретатор графа: JSON-документ → валидация → пошаговое выполнение с колбеками/стримом событий.

Установка из этой папки:

```bash
pip install -e .
```

Запуск CLI-заглушки:

```bash
python -m graph_caster --help
```
