<!-- Copyright GraphCaster. All Rights Reserved. -->

# Example graphs

Copy a `*.json` file into your workspace **`graphs/`** directory (or open it from the UI) to try a scenario.

| File | Description |
|------|-------------|
| `cursor-agent-linear.json` | Start → **task** with **`gcCursorAgent`** → Exit. Requires **`GC_CURSOR_AGENT`** (or Cursor CLI on `PATH`). See **`python/README.md`**. |

Canonical test fixture (same shape, used by pytest): **`schemas/test-fixtures/cursor-agent-linear.json`**.
