// Copyright GraphCaster. All Rights Reserved.

/** i18n key under `app.errors.gc.*` for a structured GraphCaster ``gcCode`` from the runner. */
export function gcErrorTranslationKey(code: string): string {
  return `app.errors.gc.${code}`;
}
