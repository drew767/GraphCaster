// Stub for html-to-image — replace with real package when added to package.json
export async function toPng(_el: HTMLElement, _opts?: unknown): Promise<string> {
  return "data:image/png;base64,";
}
